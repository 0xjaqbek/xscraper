{
  "name": "twitter-selective-reply-bot",
  "version": "1.0.0",
  "description": "Twitter bot with AI-powered selective replies",
  "main": "working-dashboard.js",
  "scripts": {
    "start": "node working-dashboard.js",
    "build": "npx playwright install --with-deps chromium",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": "18.x"
  }
}